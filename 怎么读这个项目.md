# 怎么读这个项目(triton-kernels · Triton 手写 LLM 算子)

> **上游**:跨仓的先后次序见 `Resume/STUDY_MAP.md`;本文只讲**进了这个仓之后怎么读**。
> **同级**:各仓根目录的同名文件结构一致,读熟一个就会读其余五个。
> **姊妹仓**:`Kernel_Optimazation`(同一批算子的手写 CUDA 版)——
> 两仓合起来才是「什么时候该用 CUDA、什么时候该用 Triton」的完整答案。

---

## 总纲:这个仓的主线是一个判断,不是一堆 kernel

七个 Triton 算子(FA2 / 流水线 GEMM / FP8 / flash-decoding / MoE permute /
CUDA Graph / 融合逐元素)看起来是并列的,其实它们共同回答一个问题:

> **简化实现能在多大程度上逼近生产级库,差距具体来自哪一层?**

而这个问题的答案不是一个数,是**三个口径**:设备侧、launch、融合。
读这个仓如果只记住"FA2 达到 SDPA 的 87%",就等于什么都没读到 ——
真正的产出是"看到一个性能差距时,先拆哪三层"。

所以本仓的入口不是任何一个 kernel,是 `docs/theory/03_triton_vs_cuda.md`。

---

## 零、第一次进仓:按顺序打开(75 分钟)

| 步 | 打开什么 | 只看什么 | 时间 |
|---|---|---|---|
| 1 | `README.md` | 「性能结果」表 + 图 3(launch 四口径) | 10 min |
| 2 | `docs/theory/03_triton_vs_cuda.md` | **全文**。这是全仓总纲,五节速查格式,一页纸 | 15 min |
| 3 | `src/elementwise_kernels.py` | 全文(最短的行核,三个 kernel) | 15 min |
| 4 | 跑 `python scripts/test_ew_gemm.py` | 看行核在不同尺寸下与 torch 的对比 | 10 min |
| 5 | `records/EXP-T03_ports_and_binding.md` | §5 结果 + §6 分析 | 15 min |
| 6 | 白板自测 | 不看代码,写出「小尺寸下 Triton 慢 4 倍」要拆成哪三层、每层的判据是什么 | 10 min |

第 5 步是关键:EXP-T03 是把"Triton 比 CUDA 慢"这个裸命题拆成三个口径的那一份记录。
**做完这六步,你应该能在看到任何一个"A 比 B 慢"的结论时,立刻问出三个拆解问题。**

---

## 一、完整阅读顺序(精确到文件)

按**认知依赖**排,不是按 EXP 编号。每一行走完下面的「固定四步」。

| # | 主题 | 入口文件 | 走完能答的一个问题 | 配套 |
|---|---|---|---|---|
| 0 | 三口径总纲 | `docs/theory/03_triton_vs_cuda.md` | 看到性能差距先拆哪三层 | EXP-T03 |
| 1 | 行核与融合 | `src/elementwise_kernels.py` | 为什么"融合数比单核快慢更重要" | EXP-T03 |
| 2 | launch 与 Graph | `src/`(无独立文件,见 `scripts/test_cudagraph.py`) | 小 kernel 的正解为什么是 Graph 不是换语言 | EXP-T05 |
| 3 | FA2 | `src/fa2_fwd.py` | online softmax 怎么把 O(S²) 物化消掉 | **讲义 01** + theory 01 |
| 4 | GEMM 流水线 | `src/gemm_pipelined.py` | 为什么 2 级双缓冲只 +1%、3 级才 +21% | **讲义 02** + theory 02 |
| 5 | flash-decoding | `src/flash_decode.py` | 为什么它在短上下文反亏 | theory 05 + EXP-T04 |
| 6 | FP8 | `src/fp8_gemm.py` | 2× 理论收益为什么在 Ada 上只吃到 1.5× | theory 06 |
| 7 | MoE permute | `src/moe_permute.py` | scatter-add 翻转成 gather 为什么值 12.5× | theory 07 |
| 8 | 融合逐元素 | `src/llm_fused.py` | 访存主导算子上 Triton 与 CUDA 为什么打平 | EXP-T09 + 姊妹仓讲义 03 |

三个里程碑,走到就停下做白板自测(标准见第四章):

- 走完 **#2**:三口径方法论齐了。这是本仓最可迁移的东西。
- 走完 **#4**:能读懂任何一个 Triton tile kernel 的结构。
- 走完 **#8**:能回答"什么时候该用 CUDA 而不是 Triton"(配合姊妹仓的三点曲线)。

### 每个算子的固定四步(20-40 分钟)

| 步 | 动作 |
|---|---|
| 1 | 读 `docs/theory/NN_*.md` 对应篇的**前两节**(一句话结论 + 机制) |
| 2 | 读 `src/*.py` 的**模块 docstring 与 kernel 函数头注释** |
| 3 | 跑 `scripts/test_*.py`,看实际输出 |
| 4 | 读 `records/EXP-TNN` 的 §6(分析)与 **§7(异常与开放问题)** |

只有第 4 步卡住了,才去开 `docs/lectures/` 的对应小节。

---

## 二、文档分层:按目的进

本仓的四层是全区最完整的(**七篇 theory 速查层**是别的仓没有的)。

| 你的目的 | 进哪一层 | 时间 |
|---|---|---|
| 知道有什么、结论是什么 | `README.md` 性能结果表 + 关键发现 | 10 min |
| 某个机制的一页纸速查 | **`docs/theory/NN_*.md`**(五节:结论/机制/本项目实证/追问 Q&A/延伸) | 10 min/篇 |
| 某个机制要真的学会 | `docs/lectures/` 的**对应小节**(三篇,单篇 900-1000 行,不要通读) | 30-60 min |
| 查数字怎么来的、可不可信 | `records/EXP-TNN` 的 §2 / §5 / **§7** | 10 min |
| 练面试怎么讲 | `docs/talk/triton_kernels_talk.md` + 白板卡 | 15 min |
| 查当时为什么这么决定 | `LAB_JOURNAL.md` / `LEDGER.md` | 按需 |

**本仓特有的一条纪律**:`docs/theory/` 是入口,`docs/lectures/` 是深水区。
七篇 theory 每篇一页纸、五节固定格式,**通勤路上就能过一遍**;讲义三篇是坐下来才读的。
其余仓缺 theory 层,所以进法不同 —— 这是本仓最舒服的地方,别浪费。

---

## 三、代码怎么看:Triton 与 CUDA 的读法不同

### 读法一 · Triton kernel 可以整篇读,但要先读 grid

Triton kernel 都很短(FA2 前向 80 行),不像 CUDA 那样需要 diff 读版本梯。
但顺序仍然是**从外往里**:

1. **先读 Python 包装函数最后的 `kernel[(grid,)](...)` 那一行** ——
   grid 的形状决定了"一个 program 负责什么"。
2. **再读 kernel 签名里的 `tl.constexpr` 参数** ——
   `BLOCK_M/BLOCK_N/BLOCK_K`、`num_stages`、`num_warps` 是编译期常量,
   它们不是"调参",是**决定生成什么代码**的。
3. **最后读 kernel 体**。

以 `src/gemm_pipelined.py` 为例:不先看到 `num_stages` 是 constexpr,
就理解不了"只改这一个数为什么能 +21%"。

### 读法二 · 看不懂性能时,直接 dump PTX

这是本仓相对 CUDA 仓的一个优势:Triton 的编译产物可以直接拿到。

```python
k = my_kernel[(grid,)](...)          # 返回编译后的 kernel 对象
ptx = k.asm['ptx']
import re; from collections import Counter
print(Counter(re.findall(r'(ld\.global\S*|st\.global\S*)', ptx)))
```

看到 `ld.global.v4.b32` 就是 16 字节向量访存,看到 `ld.global.b16` 就是标量 ——
**"我以为它向量化了"和"它真的向量化了"之间,只隔这三行代码**。
本仓的 rope kernel 就是靠这一步排除了"没向量化"的猜测(EXP-T09 §7)。

### 读法三 · 三个口径分开测,不要只测一个

本仓所有 bench 都遵循这个:同一个算子要分别给出

1. **设备侧**(CUDA event 包 kernel,不含 launch)
2. **launch 侧**(Python 分发开销,~30µs vs torch C++ ~8µs vs 裸 CUDA ~5µs)
3. **端到端**(含融合带来的 kernel 数变化)

读 `scripts/test_*.py` 时重点看它怎么把这三层分开 —— **这是本仓最该抄走的工程习惯**。

### 读法四 · 读 `tl.` 语义时同时问"它保证什么、不保证什么"

典型例子(EXP-T09 §7 实录):`tl.load(..., mask=m)` 保证**语义正确**
(被屏蔽的 lane 不写、读到 `other`),**不保证被屏蔽的那一路不产生实际访存事务**。
本仓实测:把 q/k 合进一个 kernel 用 mask 选择,有效带宽恰好减半 —— 两路都发了事务。

读 DSL 的时候,**把"文档保证的"和"你以为它顺带保证的"分开**,是最容易踩坑的地方。

---

## 四、自测验收

不看代码,在白纸上做:

| 里程碑 | 验收动作 |
|---|---|
| #2 后 | 画出三口径拆解流程图(设备侧同速吗 → 差在 launch 吗 → 端到端有几次相邻 launch) |
| #4 后 | 画出 FA2 的 online softmax 归并式,说明它省掉的是哪一份物化;说清 num_stages 与"搬运延迟/计算时长"比值的关系 |
| #8 后 | 给出"什么时候该用 Triton、什么时候必须 CUDA"的判断依据,并各举一个自家实测的例子 |

面试前一晚:过 `docs/talk/triton_kernels_talk.md` + `whiteboard_card_fa2_algebra.md`,
再过三篇讲义的**误区与边界**节。

---

## 五、当前缺口

1. **三篇讲义正在被工具链加目录**(`docs/lectures/*.md` 有未提交改动),
   读之前先确认状态。
2. **`src/llm_fused.py` 没有对应的 theory 速查篇**。其余七个主题都有,
   这一个只有姊妹仓的讲义 03。补一篇 `08_memory_bound_fusion.md` 会让 theory 层齐整。
3. EXP-T09 的数字权威在姊妹仓(同一次运行产出),本仓只有指路牌 ——
   读的时候注意 `data/raw/EXP-T09/POINTER.txt`。
